Context: codes and datasets dittoed from /sudoku_chloe.

Setting up on Mac:
1. create venv - $ python3 -m venv .venv
2. activate venc - $ source .venv/bin/activate
3. install prereq python modules - $ pip install opencv-python numpy tensorflow keras matplotlib
4. run pipeline - $ python Nadon/main.py

6/11/2025
Trained a CNN model with MNIST datasets. Actually useless. Next steps I can try:
        1. Transfer leanring + custom datasets
        2. Train on datasets of "irl" sudoku images
        3. Augmented training on existing CNN model
